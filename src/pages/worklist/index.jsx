import { primaryGray } from "../../constants/colors"

const WorkListPage = () => {
    return (<div className={`p-4 bg-[${primaryGray}] h-full w-full`} style={{padding: "10px 35px"}}>
        
    </div>)
}

export default WorkListPage;